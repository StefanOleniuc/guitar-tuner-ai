fastapi==0.115.12
uvicorn[standard]==0.34.0
python-dotenv==1.1.0
pydantic==2.11.4
pydantic-settings==2.9.1
python-multipart==0.0.29
# Client HTTP pentru SendGrid API (Cloudflare blochează urllib).
httpx==0.28.1

# Driver PostgreSQL pentru stocarea conturilor / preferințelor / istoricului.
psycopg2-binary==2.9.10

# AI pitch detection (CREPE rulează pe TensorFlow)
crepe==0.0.16
tensorflow==2.21.0
numpy>=1.26,<3

# Autentificare (email + parolă, JWT)
bcrypt==4.2.1
pyjwt==2.10.1
# Verificarea domeniului de email prin DNS (înregistrări MX)
dnspython==2.7.0

# Dev / testare
pytest==9.0.3
pytest-asyncio==1.4.0
